{
    "version": "0.2.0",
    "configurations": [
        // ... (你截图中原有的 helloworld 配置保留在这里) ...
        {
            "name": "Rust Debug (新项目名称)",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build",
                    "--manifest-path=${workspaceFolder}/your_project_name/Cargo.toml"
                ]
            },
            "args": [],
            "cwd": "${workspaceFolder}/your_project_name"
        }
    ]
}